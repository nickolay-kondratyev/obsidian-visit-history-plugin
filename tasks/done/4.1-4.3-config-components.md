# Task 4.1–4.3: Config Panel Components

> **Phase:** 4 — View Config Components
> **Depends on:** Tasks 2.1, 2.2
> **Blocks:** Task 5.3

## 4.1 — GradientPicker.tsx

**Props:**
```typescript
{
  active: string;
  onChange: (key: string) => void;
}
```

- Renders gradient swatch buttons for each entry in `GRADIENTS`.
- Active gradient has `.active` class.

## 4.2 — HeatmapOptions.tsx

**Props:**
```typescript
{
  field: string;
  setField: (f: string) => void;
  gradKey: string;
  setGradKey: (k: string) => void;
  hotDays: number;
  setHotDays: (d: number) => void;
  coldDays: number;
  setColdDays: (d: number) => void;
}
```

- Timestamp field dropdown (modified/created/visited).
- GradientPicker embedded.
- Hot/cold day range sliders.
- Gradient preview bar.
- Null color indicator.

## 4.3 — ConfigPanel/index.tsx

**Props:**
```typescript
{
  open: boolean;
  colorMode: 'type' | 'heatmap';
  setColorMode: (m: 'type' | 'heatmap') => void;
  gradKey: string;
  setGradKey: (k: string) => void;
  field: string;
  setField: (f: string) => void;
  hotDays: number;
  setHotDays: (d: number) => void;
  coldDays: number;
  setColdDays: (d: number) => void;
  scales: Record<string, number>;
  setScales: (s: Record<string, number>) => void;
}
```

- Scale factor inputs for each file type.
- Color mode toggle (By type / Heatmap).
- Conditionally renders HeatmapOptions when mode is "heatmap".
- Slides in/out with CSS transform (controlled by `open` prop).
