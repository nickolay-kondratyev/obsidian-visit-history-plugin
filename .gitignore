# vscode
.vscode 

# Intellij
*.iml
.idea

# npm
node_modules

# Don't include the compiled main.js file in the repo.
# They should be uploaded to GitHub releases instead.
main.js

# Exclude sourcemaps
*.map

# obsidian
data.json

# Exclude macOS Finder (System Explorer) View States
.DS_Store

# Gradle
.gradle
build/

*do_not_commit*
# AI tooling scratch + test screenshots (never source-controlled)
.tmp/
.out/

# e2e: installed plugin build in the seed vault (rebuilt by setup:dev-vault; not committed).
# The rest of .dev-vault (notes, canvas, minimal .obsidian config) IS committed.
.dev-vault/.obsidian/plugins/
